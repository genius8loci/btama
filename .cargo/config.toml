# Игра — 32-битный процесс, поэтому DLL обязана быть 32-битной.
# Без этого обычный `cargo build` молча собирает x86_64 и падает на
# ассемблерной вставке (или, хуже, собирается и не инжектится).
[build]
target = "i686-pc-windows-msvc"
